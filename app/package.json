{
  "name": "demo-devops-app",
  "version": "1.0.0",
  "description": "Demo app for SIT223 DevOps pipeline",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --runInBand --coverage",
    "test:junit": "jest --runInBand --coverage --reporters=default --reporters=jest-junit",
    "build": "echo 'no build step for node app'",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "prom-client": "^14.0.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "jest-junit": "^13.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./reports/junit",
    "outputName": "junit.xml"
  }
}
